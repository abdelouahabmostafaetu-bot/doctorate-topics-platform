
// ===== Mathora — مساعد الموقع: حد 50 رسالة / 4 ساعات =====
// إذا كان AssistantUsage موجودًا مسبقًا: أضف فقط الحقلين totalCount و updatedAt داخل الموديل
// ولا تعِد لصق model AssistantUsage كاملًا (تجنب التكرار)
model AssistantUsage {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  userId      String   @unique
  windowStart DateTime @default(now())
  count       Int      @default(0)
  totalCount  Int      @default(0) // إجمالي رسائل Mathora مدى الحياة (للأدمن)
  updatedAt   DateTime @updatedAt  // آخر استخدام

  @@map("assistant_usage")
}

// ===== نصائح الشريط العلوي — يتحكم فيها الأدمن من /admin/tips =====
// ألصق SiteTip فقط إذا لم يكن موجودًا
model SiteTip {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  text      String
  href      String?
  cta       String?
  active    Boolean  @default(true)
  order     Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("site_tips")
}
